# 🚀 阿里云部署指南 - 快速开始

## 📋 部署前准备清单

在开始部署前，请确保您已经：

- [ ] 拥有阿里云账号并完成实名认证
- [ ] 拥有GitHub账号
- [ ] 本地安装了Git
- [ ] 准备好域名（可选，但推荐）

---

## 第一步：购买阿里云服务（预计30分钟）

### 1.1 创建 RDS MySQL 数据库

1. 登录 [阿里云控制台](https://console.aliyun.com)
2. 搜索 "RDS" → 进入云数据库 RDS
3. 点击 "创建实例"
4. 配置选择：
   ```
   地域: 华东2（上海）
   数据库类型: MySQL
   版本: 8.0
   系列: 高可用版
   规格: mysql.n2.medium.1 (2核4GB)
   存储: 100GB SSD云盘
   网络类型: 专有网络VPC（自动创建）
   ```
5. 设置密码（务必记住！）
6. 点击 "立即购买" → 等待创建完成（约5分钟）
7. **记录连接信息**：
   ```
   内网地址: rm-xxxxx.mysql.rds.aliyuncs.com
   端口: 3306
   用户名: root
   密码: [您设置的密码]
   ```

### 1.2 创建 OSS 存储桶

1. 搜索 "OSS" → 进入对象存储 OSS
2. 点击 "创建 Bucket"
3. 配置：
   ```
   Bucket名称: songbrocade-frontend (前端)
   区域: 华东2（上海）
   存储类型: 标准存储
   读写权限: 公共读
   版本控制: 开启
   ```
4. 点击 "确定"
5. 重复步骤创建第二个Bucket：
   ```
   Bucket名称: songbrocade-media (媒体文件)
   区域: 华东2（上海）
   存储类型: 标准存储
   读写权限: 私有
   ```

### 1.3 创建 AccessKey

1. 点击右上角头像 → "AccessKey管理"
2. 点击 "创建AccessKey"
3. **立即保存**（只显示一次！）：
   ```
   AccessKey ID: LTAI5t...
   AccessKey Secret: [保密信息]
   ```

### 1.4 配置 RDS 白名单

1. 进入 RDS 实例管理
2. 左侧菜单 → "数据安全性" → "白名单设置"
3. 点击 "添加白名单分组"
4. 添加：
   ```
   分组名称: public_access
   白名单: 0.0.0.0/0
   ```
   ⚠️ 注意：生产环境应该限制为具体IP

### 1.5 创建数据库和表

1. 下载数据库客户端（推荐：[DBeaver](https://dbeaver.io/)）
2. 连接到RDS：
   ```
   主机: rm-xxxxx.mysql.rds.aliyuncs.com
   端口: 3306
   用户名: root
   密码: [您的密码]
   ```
3. 执行SQL脚本：
   ```sql
   -- 创建数据库
   CREATE DATABASE songbrocade CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   USE songbrocade;
   
   -- 复制并执行 init-db-simple.sql 中的所有SQL
   ```

---

## 第二步：配置GitHub仓库（预计15分钟）

### 2.1 创建GitHub仓库

1. 访问 [GitHub](https://github.com)
2. 点击右上角 "+" → "New repository"
3. 填写：
   ```
   Repository name: songbrocade
   Description: AI驱动的文化出海平台
   Visibility: Private (推荐) 或 Public
   ```
4. 点击 "Create repository"

### 2.2 推送代码到GitHub

```bash
# 在项目目录下执行
cd /Users/petterbrand/Downloads/阿里云

# 初始化Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 准备部署到阿里云"

# 添加远程仓库（替换为您的仓库地址）
git remote add origin https://github.com/YOUR_USERNAME/songbrocade.git

# 推送到GitHub
git push -u origin main
```

### 2.3 配置GitHub Secrets

1. 进入仓库 → Settings → Secrets and variables → Actions
2. 点击 "New repository secret"
3. 添加以下Secrets（一个一个添加）：

#### 阿里云基础配置
```
名称: ALIYUN_ACCOUNT_ID
值: [您的阿里云账号ID，在控制台右上角可以找到]

名称: ALIYUN_ACCESS_KEY_ID
值: LTAI5t... [步骤1.3中获取的]

名称: ALIYUN_ACCESS_KEY_SECRET
值: [步骤1.3中获取的Secret]
```

#### OSS配置
```
名称: OSS_REGION
值: oss-cn-shanghai

名称: OSS_BUCKET
值: songbrocade-frontend

名称: OSS_BUCKET_MEDIA
值: songbrocade-media
```

#### 数据库配置
```
名称: DB_HOST
值: rm-xxxxx.mysql.rds.aliyuncs.com [您的RDS地址]

名称: DB_PORT
值: 3306

名称: DB_USER
值: root

名称: DB_PASSWORD
值: [您的RDS密码]

名称: DB_NAME
值: songbrocade
```

#### 区块链配置（保持现有的）
```
名称: RPC_URL
值: https://sepolia.base.org

名称: BROCADE_ADDR
值: [您的合约地址]

名称: RDA_REG_ADDR
值: [您的合约地址]
```

#### AI服务配置（保持现有的）
```
名称: OPENAI_API_KEY
值: sk-... [您的OpenAI API Key]

名称: REPLICATE_API_KEY
值: r8_... [您的Replicate API Key]
```

#### 业务配置
```
名称: ADMIN_WALLETS
值: 0xEf85456652ada05f12708b9bDcF215780E780D18

名称: SHIPPING_KEY
值: ir9I4xwi1Umc9W2jSv6NUB9LCjzhufhixOpMvPUR02U=
```

#### CDN配置（可选）
```
名称: CDN_DOMAIN
值: www.songbrocade.com [如果有域名]
```

---

## 第三步：修改代码适配阿里云（预计1小时）

### 3.1 创建数据库适配层

创建文件 `worker-api/utils/db-mysql.js`:

```javascript
import mysql from 'mysql2/promise';

let pool;

export function initPool(env) {
  if (pool) return pool;
  
  pool = mysql.createPool({
    host: env.DB_HOST,
    port: parseInt(env.DB_PORT || '3306'),
    user: env.DB_USER,
    password: env.DB_PASSWORD,
    database: env.DB_NAME,
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0,
    enableKeepAlive: true,
    keepAliveInitialDelay: 0
  });
  
  console.log('✅ MySQL连接池已创建');
  return pool;
}

export async function query(sql, params = []) {
  if (!pool) {
    throw new Error('数据库连接池未初始化');
  }
  const [rows] = await pool.execute(sql, params);
  return rows;
}

export async function run(sql, params = []) {
  if (!pool) {
    throw new Error('数据库连接池未初始化');
  }
  const [result] = await pool.execute(sql, params);
  return result;
}

export async function ensureSchema() {
  console.log('✅ MySQL schema ready');
}

export async function closePool() {
  if (pool) {
    await pool.end();
    pool = null;
    console.log('✅ MySQL连接池已关闭');
  }
}
```

### 3.2 创建OSS适配层

创建文件 `worker-api/utils/oss-client.js`:

```javascript
import OSS from 'ali-oss';

let ossClient;

export function initOSS(env) {
  if (ossClient) return ossClient;
  
  ossClient = new OSS({
    region: env.OSS_REGION,
    accessKeyId: env.OSS_ACCESS_KEY_ID,
    accessKeySecret: env.OSS_ACCESS_KEY_SECRET,
    bucket: env.OSS_BUCKET
  });
  
  console.log('✅ OSS客户端已创建');
  return ossClient;
}

export async function putObject(key, buffer, options = {}) {
  if (!ossClient) {
    throw new Error('OSS客户端未初始化');
  }
  return await ossClient.put(key, buffer, options);
}

export async function getObject(key) {
  if (!ossClient) {
    throw new Error('OSS客户端未初始化');
  }
  return await ossClient.get(key);
}

export async function getSignedUrl(key, expires = 3600) {
  if (!ossClient) {
    throw new Error('OSS客户端未初始化');
  }
  return ossClient.signatureUrl(key, { expires });
}

export async function deleteObject(key) {
  if (!ossClient) {
    throw new Error('OSS客户端未初始化');
  }
  return await ossClient.delete(key);
}
```

### 3.3 修改主入口文件

修改 `worker-api/index.js` 开头部分：

```javascript
// 在文件开头添加
import { initPool, query, run, ensureSchema } from './utils/db-mysql.js';
import { initOSS, putObject, getSignedUrl } from './utils/oss-client.js';

// 初始化函数（在导出handler之前）
let initialized = false;

function initialize(env) {
  if (initialized) return;
  
  // 初始化数据库连接池
  initPool(env);
  
  // 初始化OSS客户端
  initOSS(env);
  
  initialized = true;
  console.log('✅ 服务初始化完成');
}

// 修改原来的fetch handler
export default {
  async fetch(request, env, ctx) {
    // 初始化
    initialize(env);
    
    // 原来的代码...
  }
};

// 如果使用函数计算，添加以下导出
export async function handler(event, context) {
  // 从环境变量初始化
  initialize(process.env);
  
  // 转换FC事件为标准Request
  const request = new Request(event.url || 'http://localhost', {
    method: event.httpMethod || event.method || 'GET',
    headers: event.headers || {},
    body: event.body
  });
  
  // 调用原来的fetch逻辑
  const response = await this.fetch(request, process.env, {});
  
  return {
    statusCode: response.status,
    headers: Object.fromEntries(response.headers),
    body: await response.text()
  };
}
```

### 3.4 安装依赖

```bash
cd worker-api
npm install mysql2 ali-oss
```

### 3.5 更新前端API地址

修改 `frontend/common/api.js`:

```javascript
// 根据环境自动选择API地址
const API_BASE = window.location.hostname.includes('localhost') 
  ? 'http://localhost:8787'  // 本地开发
  : 'https://api.songbrocade.com';  // 生产环境（需要配置域名）
  // 或者使用FC的HTTP触发器地址
```

---

## 第四步：测试本地运行（预计30分钟）

### 4.1 创建本地环境变量文件

创建 `worker-api/.env.local`:

```bash
# 数据库配置
DB_HOST=rm-xxxxx.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=songbrocade

# OSS配置
OSS_REGION=oss-cn-shanghai
OSS_BUCKET=songbrocade-media
OSS_ACCESS_KEY_ID=LTAI5t...
OSS_ACCESS_KEY_SECRET=your_secret

# 其他配置（从现有配置复制）
RPC_URL=https://sepolia.base.org
BROCADE_ADDR=0x...
RDA_REG_ADDR=0x...
OPENAI_API_KEY=sk-...
REPLICATE_API_KEY=r8_...
ADMIN_WALLETS=0xEf85456652ada05f12708b9bDcF215780E780D18
SHIPPING_KEY=ir9I4xwi1Umc9W2jSv6NUB9LCjzhufhixOpMvPUR02U=
```

### 4.2 测试后端

```bash
cd worker-api

# 安装依赖
npm install

# 启动本地服务器
node index.js

# 在另一个终端测试
curl http://localhost:8787/health
```

### 4.3 测试前端

```bash
cd frontend

# 启动简单HTTP服务器
python3 -m http.server 8080

# 访问 http://localhost:8080
```

---

## 第五步：部署到阿里云（预计30分钟）

### 5.1 提交代码触发自动部署

```bash
# 确保所有修改已提交
git add .
git commit -m "feat: 适配阿里云部署"
git push origin main
```

### 5.2 查看部署进度

1. 访问 GitHub 仓库
2. 点击 "Actions" 标签
3. 查看正在运行的工作流
4. 等待部署完成（约5-10分钟）

### 5.3 验证部署

#### 前端验证
```bash
# 访问OSS直接地址
curl https://songbrocade-frontend.oss-cn-shanghai.aliyuncs.com/index.html

# 或在浏览器打开
open https://songbrocade-frontend.oss-cn-shanghai.aliyuncs.com
```

#### 后端验证
```bash
# 需要先获取FC的HTTP触发器地址
# 在阿里云控制台 → 函数计算 → songbrocade-api → api-handler → 触发器
# 复制HTTP触发器URL，然后测试

curl https://xxxxx.cn-shanghai.fc.aliyuncs.com/.../health
```

---

## 第六步：配置自定义域名（可选，预计30分钟）

### 6.1 域名备案

⚠️ **重要**：如果使用国内CDN，域名必须备案（需15-20天）

1. 进入阿里云控制台 → ICP备案
2. 按照流程完成备案

### 6.2 配置前端域名

1. 进入 CDN 控制台
2. 添加加速域名：`www.songbrocade.com`
3. 源站类型：OSS域名
4. 源站域名：`songbrocade-frontend.oss-cn-shanghai.aliyuncs.com`
5. 配置HTTPS证书（可申请免费证书）

### 6.3 配置后端域名

1. 进入函数计算控制台
2. 选择服务：songbrocade-api
3. 自定义域名 → 添加
4. 域名：`api.songbrocade.com`
5. 路径：`/` → `api-handler`

### 6.4 配置DNS解析

1. 进入域名DNS管理
2. 添加记录：
   ```
   类型: CNAME
   主机记录: www
   记录值: [CDN提供的CNAME]
   
   类型: CNAME
   主机记录: api
   记录值: [FC提供的CNAME]
   ```

---

## 🎉 部署完成！

### 访问您的应用

- **前端**: https://songbrocade-frontend.oss-cn-shanghai.aliyuncs.com
- **后端**: [FC HTTP触发器地址]
- **自定义域名**（如已配置）:
  - 前端: https://www.songbrocade.com
  - 后端: https://api.songbrocade.com

### 下一步

1. ✅ 测试所有功能
2. ✅ 配置监控告警
3. ✅ 设置自动备份
4. ✅ 优化性能
5. ✅ 编写运维文档

---

## 🆘 常见问题

### Q1: GitHub Actions部署失败？

**检查**:
1. Secrets是否配置正确
2. 查看Actions日志找到具体错误
3. 检查阿里云服务是否正常

### Q2: 数据库连接失败？

**检查**:
1. RDS白名单是否配置
2. 密码是否正确
3. 网络是否可达

### Q3: OSS上传失败？

**检查**:
1. AccessKey是否正确
2. Bucket名称是否正确
3. 权限是否足够

### Q4: 前端页面打不开？

**检查**:
1. OSS Bucket是否设置为公共读
2. 文件是否上传成功
3. 浏览器控制台是否有错误

---

## 📞 获取帮助

如果遇到问题：

1. 查看 GitHub Actions 日志
2. 查看阿里云控制台日志
3. 查看本项目的其他文档：
   - `阿里云部署方案.md`
   - `阿里云部署任务清单.md`
   - `GitHub自动部署方案.md`

---

**祝部署顺利！** 🚀

