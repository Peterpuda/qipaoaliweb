# === 网络配置 ===
# Base Sepolia 的 RPC 节点，可继续使用 base.org 官方公共节点或你自己的 Infura/Alchemy/QuickNode 端点
RPC_URL=https://sepolia.base.org

# 部署者私钥（与之前上链与登记系统相同的钱包；必须以 0x 开头）
PK=0x7b09599c87fa7b49f2d02ec57c06f04ad7d2b03cc5bbf8c5c77fbb56abc2e4ba

# === Merkle Distributor 参数 ===
# 部署合约时的 Merkle 根（从 Worker 的 v2 root 接口获取）
MERKLE_ROOT=0x17857e2c33cf368255fc6296f3839c1d6a42a56e0c0b3edc50895f91dbfde774

# === 可选：标记你在链上已存在的合约地址（部署成功后可更新这两个值） ===
BROCADE_ADDR=0x1DEC118aA1C221A71Fa9FBD4576174F679528b3d
RDA_REG_ADDR=0xed4B4Ddc123C4b3c845a92d5121603b8184CB412

# === 环境信息（用于 hardhat.config.js 读取） ===
# 你可以在 hardhat.config.js 中用 process.env.RPC_URL 和 process.env.PK 读取这些参数
# Chain ID for Base Sepolia = 84532
CHAIN_ID=84532
NETWORK=baseSepolia

# === 安全提醒 ===
# 请勿上传此文件至任何公共仓库。
# 确保文件权限仅限当前用户读取：
#   chmod 600 .env
# 并在 .gitignore 中添加：
#   .env