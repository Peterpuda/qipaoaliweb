{
  "name": "merkle-contract",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy.js --network baseSepolia",
    "verify:base": "hardhat run scripts/verify.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}
