# 前端部署说明

## ✅ 已完成的更新

1. **管理后台首页** (`/admin/index.html`)
   - 添加了 Merkle Tree 生成模块
   - 添加了侧边栏导航链接

2. **Merkle 生成页面** (`/admin/merkle.html`)
   - 修复了 token 认证逻辑
   - 支持 slug 输入

3. **首页** (`/index.html`)
   - 重新设计的现代风格
   - 添加了管理员入口
   - 领取通证按钮链接到签到页面

## 🚀 部署命令

### 方式 1：使用 Cloudflare Pages（推荐）

如果你的项目已连接到 GitHub，Cloudflare Pages 会自动部署。

### 方式 2：手动部署

```bash
cd frontend
npx wrangler pages deploy . --project-name=songbrocade-frontend --branch prod --commit-dirty=true
```

### 方式 3：使用部署脚本

```bash
./deploy-frontend.sh
```

## 📋 部署检查清单

部署完成后，请验证以下功能：

- [ ] 访问首页：https://songbrocade-frontend.pages.dev
- [ ] 点击右上角"管理员"链接
- [ ] 连接钱包登录管理后台
- [ ] 在管理后台首页看到"🎁 Merkle Tree 生成"模块
- [ ] 点击"生成 Merkle Tree"按钮
- [ ] 成功访问 `/admin/merkle.html` 页面

## 🎯 部署后操作

### 1. 测试 Merkle 生成功能

```
1. 访问 /admin/
2. 连接管理员钱包
3. 点击 "Merkle Tree 生成"
4. 输入 "airdrop-2025"
5. 点击 "加载活动信息"
6. 点击 "生成 Merkle Tree"
7. 记录 Merkle Root
```

### 2. 部署 MerkleDistributor 合约

使用生成的 Merkle Root 部署合约（参见 `AIRDROP_CLAIM_GUIDE.md`）

### 3. 配置前端合约地址

编辑 `frontend/poap.config.js`：
```javascript
DISTRIBUTOR_CONTRACT: "0x你的合约地址"
```

重新部署前端。

### 4. 测试完整流程

```
1. 访问首页
2. 点击 "领取通证" 按钮
3. 自动跳转到签到页面
4. 连接钱包
5. 点击 "铭刻我的到场"
6. 签到成功后点击 "领取 1000 枚代币"
7. 验证代币到账
```

## 🔍 常见问题

### Q: 部署后访问 /admin/merkle.html 还是跳转回首页？

**A**: 这通常是浏览器缓存问题
```bash
# 清除缓存
Ctrl+Shift+R (Windows/Linux)
Cmd+Shift+R (Mac)

# 或在开发者工具中勾选 "Disable cache"
```

### Q: 生成 Merkle Tree 时提示未登录？

**A**: Token 已过期，需要重新登录
```
1. 访问 /admin/
2. 退出登录
3. 重新连接钱包登录
4. 再次尝试生成
```

### Q: 找不到活动？

**A**: 确认活动已创建
```bash
# 访问 API 确认
curl https://songbrocade-api.petterbrand03.workers.dev/api/events/get?slug=airdrop-2025
```

---

**准备部署**: ✅  
**后端已部署**: ✅  
**等待前端部署**: ⏳

